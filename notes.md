As a PHP/Laravel backend developer using Node here (with help from an AI agent), I’d next build a reliable server-side funtionality that validates and normalizes the three spreadsheets, runs imports transactionally, and produces row-level error reports. Move storage from SQLite to MySQL for scale, migrations, and audit history. Add a small backend API with background jobs for parsing and recalculation, plus basic auth so HR and leadership can access appropriate reports and CSV exports so margins can be shared each Monday with all team members. 
And the missing parts are better UI and test cases here. Those will also improved in next level of changes due to running out of the time. 